# Server Configuration
server.port=8081
# Context path - comment out for external Tomcat deployment (context path will be WAR filename)
# For embedded Tomcat: uncomment this line

# For external Tomcat: deploy WAR as "ayurvedacare.war" or remove this line

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_ayurveda?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Siddu@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hikari Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.hbm2ddl.auto=update

# JSP View Configuration
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB
file.upload-dir=uploads

# Static Resources - handled by WebConfig

# Disable Whitelabel Error Page
server.error.whitelabel.enabled=false

# Logging
logging.level.com.ayurveda=DEBUG
logging.level.org.springframework.security=DEBUG

# DevTools
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# Razorpay Configuration (Test Mode)
# Get your keys from: https://dashboard.razorpay.com/app/keys
razorpay.key.id=rzp_test_RIlD5bEKRjyn3h
razorpay.key.secret=Ltg6uo9vI8TiFMVfj2cGm4I8

# Product Module Settings
product.default-commission=10
product.min-payout-threshold=500

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kishorhavanur07@gmail.com
spring.mail.password=nuvofenrclqostay
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Application URL (for password reset links)
app.base-url=http://localhost:8081
